================================================================================
PHASE E — ISPConfig installation REPORT
Date: 2026-08-27
Server: 80.241.223.82
Result: PASS (with known follow-ups — no tenant migration)
================================================================================

PLAN DOC
  docs/PHASE_E_ISPCONFIG_INSTALL_PLAN.txt

APPROACH
  Rejected blind get.ispconfig.org Perfect-Server autoinstaller on this dirty
  production host.
  Used official stable tarball ISPConfig 3.3.1p1 + expert autoinstall.ini:
    nginx web, configure_mail=n, configure_dns=n, configure_firewall=n,
    panel port 8081 (8080 kept for quizsnap Laravel Reverb).

PRE-INSTALL FREEZE
  /srv/backups/phase-e-pre-install-20260827T194250Z/
  Secrets (root-only, not in git): /root/ifnotus-secrets/phase-e-ispconfig.txt

INSTALL LOG
  /var/log/ispconfig-phase-e-install.log
  Ended: "Installation completed."

POST-INSTALL FIXES (required for health)
  1) nginx failed: apps.vhost and ispconfig.vhost both listened on 8081 with
     duplicate ipv6only options. Moved apps vhost to 8082; nginx restarted.
  2) Panel hung: MySQL user ispconfig was created for hostname
     vmi3379516.contaboserver.net while app connects to 127.0.0.1.
     Recreated/granted ispconfig@localhost and ispconfig@127.0.0.1.
  3) MySQL validate_password component uninstalled (installer requirement).
  4) UFW: allow 8081/tcp (panel), 8082/tcp (apps).

VERIFICATION
  [PASS] /usr/local/ispconfig present (3.3.1p1)
  [PASS] Panel https://80.241.223.82:8081 → login page HTTP 200 (self-signed TLS)
  [PASS] Remote API path present: .../interface/web/remote/index.php
  [PASS] dbispconfig exists (78 tables)
  [PASS] nginx active; ifnotus.space / cpanel / mail → 200
  [PASS] mysql, postgresql, named, postfix, dovecot, redis, ifnotus-api active
  [PASS] Tenant tree /srv/apps/ifnotus-customers intact
  [PASS] Product apps (e.g. quizsnap) present
  [PASS] No tenants migrated into ISPConfig
  [NOTE] Pure-FTPd now active (ISPConfig configured it; Phase D policy was
         packages kept / daemon off until engine needs it — now needed)
  [NOTE] Panel cert is self-signed (LE for Contabo FQDN mismatched public IP)
  [NOTE] Admin credentials only in /root/ifnotus-secrets/phase-e-ispconfig.txt

ADMIN ACCESS POLICY
  Infrastructure-only: https://80.241.223.82:8081
  Do NOT publish as customer cPanel.
  Customer path remains domain.tld/cpanel → IFNOTUS /go/hosting.

OUT OF SCOPE (STOP — do not continue without “next phase”)
  Phase F — dedicated remote API user + IFNOTUS env credentials
  Later — tenant migration, DNS/mail cutover, default provider flip

================================================================================
END REPORT
================================================================================
